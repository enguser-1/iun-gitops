apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Bootstrap : ordre = Subscription operator -> AppProject -> root App
# Appliquer manuellement après que le CRD Application soit prêt
# (cf. README §"Bootstrap Argo CD").
resources:
  - 01-openshift-gitops-subscription.yaml
  - 02-root-app-project.yaml
  - 03-root-application.yaml


