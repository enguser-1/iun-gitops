# RECONCILED FROM LIVE 2026-07-13 (sync-prod-gitops.ps1)
# NODE_ENV=production + ELASTIC_APM_ACTIVE=false + env completes (envalid).
# auth/search : logger-fix monte en subPath (cf. ../18-logger-recursion-fix.yaml).
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    iun.sn/image-tag-verified: 2026-06-03 ΓÇö ghcr.io/opencrvs/ocrvs-webhooks:v1.9.13
  labels:
    app.kubernetes.io/component: opencrvs
    app.kubernetes.io/name: webhooks
  name: webhooks
  namespace: iun-opencrvs-dev
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app.kubernetes.io/name: webhooks
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      annotations:
        openshift.io/required-scc: iun-anyuid-caps
      labels:
        app.kubernetes.io/component: opencrvs
        app.kubernetes.io/name: webhooks
    spec:
      containers:
      - env:
        - name: NODE_ENV
          value: production
        - name: HOST
          value: 0.0.0.0
        - name: PORT
          value: "2525"
        - name: MONGO_APP_USER
          valueFrom:
            secretKeyRef:
              key: app-username
              name: mongodb-creds
        - name: MONGO_APP_PASSWORD
          valueFrom:
            secretKeyRef:
              key: app-password
              name: mongodb-creds
        - name: MONGO_URL
          value: mongodb://app:yJajKfi9dHz91tzDGQHuhDIAKiqHAGEy@mongo-headless:27017/webhooks?authSource=user-mgnt
        - name: AUTH_URL
          value: http://auth:4040
        - name: USER_MANAGEMENT_URL
          value: http://user-mgnt:3030
        - name: COUNTRY_CONFIG_URL
          value: http://countryconfig:3040
        - name: HMAC_WEBHOOK_SIGNING_SECRET
          valueFrom:
            secretKeyRef:
              key: hmac-webhook-signing-secret
              name: countryconfig-creds
        - name: CERT_PUBLIC_KEY_PATH
          value: /secrets/jwt-public.pem
        - name: NODE_OPTIONS
          value: --max-old-space-size=2048 --dns-result-order=ipv4first
        - name: CHECK_INVALID_TOKEN
          value: "true"
        - name: FHIR_URL
          value: http://hearth:3447/fhir
        - name: REDIS_HOST
          value: redis
        - name: ELASTIC_APM_ACTIVE
          value: "false"
        image: ghcr.io/opencrvs/ocrvs-webhooks:v1.9.13
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 6
          httpGet:
            path: /ping
            port: 2525
            scheme: HTTP
          initialDelaySeconds: 60
          periodSeconds: 20
          successThreshold: 1
          timeoutSeconds: 5
        name: webhooks
        ports:
        - containerPort: 2525
          name: http
          protocol: TCP
        readinessProbe:
          failureThreshold: 6
          httpGet:
            path: /ping
            port: 2525
            scheme: HTTP
          initialDelaySeconds: 20
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 5
        resources:
          limits:
            cpu: "1"
            memory: 2Gi
          requests:
            cpu: 100m
            memory: 512Mi
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        volumeMounts:
        - mountPath: /secrets
          name: jwt-keys
          readOnly: true
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext:
        fsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      serviceAccount: default
      serviceAccountName: default
      terminationGracePeriodSeconds: 30
      volumes:
      - name: jwt-keys
        secret:
          defaultMode: 256
          secretName: jwt-keys
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: opencrvs
    app.kubernetes.io/name: webhooks
  name: webhooks
  namespace: iun-opencrvs-dev
spec:
  clusterIP: 172.30.203.58
  clusterIPs:
  - 172.30.203.58
  internalTrafficPolicy: Cluster
  ipFamilies:
  - IPv4
  ipFamilyPolicy: SingleStack
  ports:
  - name: http
    port: 2525
    protocol: TCP
    targetPort: 2525
  selector:
    app.kubernetes.io/name: webhooks
  sessionAffinity: None
  type: ClusterIP
