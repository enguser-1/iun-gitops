# apps/iun-api — Déprécié (Approche B, 2026-05-23)

Ce dossier est conservé pour ne pas casser des références externes éventuelles.

La nouvelle structure (Approche B) place :

- les **Applications Argo CD IUN-spécifiques** dans `../iun/` (aggregator),
- la **base Kustomize** de l'API IUN dans `../../components/iun-api/base/`.

Cf. `../iun/README.md` et `../../README.md` §0.
