apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# ============================================================================
# Overlay staging — Approche DPG (pivot 2026-05-24)
# ============================================================================
# Iso-prod côté périmètre Applications, dimensionnement pour tests de charge
# 50k TPS (TOR §5.5 P1) et pilote Dakar+Thiès 50k enrôlés (TOR §5.5 P2).
# Operators socle = prérequis externes.
# ============================================================================
namespace: iun-gitops

resources:
  - ../../apps/dpg/mosip
  - ../../apps/dpg/opencrvs
  - ../../apps/dpg/openimis
