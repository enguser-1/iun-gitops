apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Overlay staging (Approche B) : iso-prod côté applications IUN, ressources
# dimensionnées pour tests de charge.
# Operators socle = prérequis externes (cf. apps/iun/README.md).
namespace: iun-gitops
resources:
  - ../../apps/iun
