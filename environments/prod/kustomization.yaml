apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Overlay prod : channels figés sur versions LTS, installPlanApproval = Manual recommandé.
namespace: openshift-gitops
resources:
  - ../../apps/socle
# Pour figer les upgrades en prod, patcher toutes les Subscriptions :
# patches:
#   - target:
#       kind: Subscription
#     patch: |-
#       - op: replace
#         path: /spec/installPlanApproval
#         value: Manual


