apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# ============================================================================
# Overlay prod — Approche DPG (pivot 2026-05-24)
# ============================================================================
# Iso-staging côté périmètre Applications, ressources et politiques figées
# (revisionHistoryLimit augmenté, selfHeal toujours faux jusqu'à validation
# manuelle de chaque promotion).
# ============================================================================
namespace: iun-gitops

resources:
  - ../../apps/dpg/mosip
  - ../../apps/dpg/opencrvs
  - ../../apps/dpg/openimis

# Patches typiques prod (à activer en phase P3) :
# patches:
#   - target:
#       kind: Application
#       name: mosip-bootstrap-probe
#     patch: |-
#       - op: replace
#         path: /spec/source/targetRevision
#         value: 1.2.1.0  # version LTS figée
