apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Overlay dev : agrège toutes les Applications du socle telles quelles.
# Ajouter ici des patches (channels en preview, replicas réduits, etc.) si besoin.
namespace: openshift-gitops
resources:
  - ../../apps/socle
# Exemple de patch pour rétrograder la sub OpenShift Pipelines en canal "preview" :
# patches:
#   - target:
#       kind: Subscription
#       name: openshift-pipelines-operator-rh
#     patch: |-
#       - op: replace
#         path: /spec/channel
#         value: preview


