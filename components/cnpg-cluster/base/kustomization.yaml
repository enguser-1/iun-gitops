apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Placeholder — Cluster PostgreSQL IUN via CloudNativePG.
# CNPG Operator déjà installé sur le cluster (autre équipe).
# À remplir phase 2 :
#   - namespace.yaml          (iun-data)
#   - cluster.yaml            (kind: Cluster, postgresql.cnpg.io/v1)
#   - backup.yaml             (kind: ScheduledBackup, S3/ODF)
#   - poolers.yaml            (PgBouncer si nécessaire)
resources: []
