apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Placeholder — Realm Keycloak IUN via RHBK.
# RHBK Operator déjà installé sur le cluster (autre équipe).
# À remplir phase 2 :
#   - namespace.yaml             (iun-iam)
#   - keycloak.yaml              (kind: Keycloak, k8s.keycloak.org/v2alpha1)
#   - realm-iun.yaml             (kind: KeycloakRealmImport ou KeycloakRealm)
#   - clients/*.yaml             (clients OIDC: iun-api, argocd, etc.)
resources: []
