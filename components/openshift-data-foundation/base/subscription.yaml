# OpenShift Data Foundation 4.16 — Operator #8 du socle (Ceph + NooBaa)
# Fournit le stockage SDS + S3 Object Lock pour archivage immuable.
---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: odf-operator
  namespace: openshift-storage
spec:
  channel: stable-4.16
  installPlanApproval: Automatic
  name: odf-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace


